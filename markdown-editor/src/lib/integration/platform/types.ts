// Re-export PlatformAdapter and related types from the central types module
export type {
  PlatformAdapter,
  ArticlePayload,
  PublishResult,
  ConnectionTestResult,
  LocalImageRef,
  ImageUploadResult,
} from '$lib/types/platform'
